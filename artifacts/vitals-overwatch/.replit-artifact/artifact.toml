kind = "web"
previewPath = "/"
title = "Vitals Overwatch"
version = "1.0.0"
id = "artifacts/vitals-overwatch"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24308

[services.development]
run = "pnpm --filter @workspace/vitals-overwatch run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/vitals-overwatch", "run", "build" ]
publicDir = "artifacts/vitals-overwatch/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24308"
BASE_PATH = "/"
